/* 99 Club Studio school-usage telemetry configuration.
 * Kept separate from Google Analytics so parent-practice pages can remain free
 * of GA/cookie code. This scaffold is intentionally OFF until the final
 * school-usage analytics/privacy design is approved.
 */
window.TT99_SCHOOL_USAGE_CONFIG = Object.freeze({
  enabled: false,
  endpoint: '',
  schemaVersion: 2
});
